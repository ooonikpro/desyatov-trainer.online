---

---

<ol class="stepper">
  <slot />
</ol>

<style lang="scss">
  .stepper {
    --stepper-row-gap: 3rem;

    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--stepper-row-gap);

    @include desktop {
      --stepper-row-gap: 3.2rem;
    }
  }
</style>
